<%= form_with(model: @cupom, local: true) do |form| %>
    <% if @cupom.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@cupom.errors.count, "error") %> prohibited this cupom from being saved:</h2>
  
        <ul>
          <% @cupom.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="field">
      <%= form.label :nome %>
      <%= form.text_field :nome %>
    </div>
  
    <div class="field">
      <%= form.label :validade %>
      <%= form.date_field :validade %>
    </div>
  
    <div class="field">
      <%= form.label :percentual_desconto %>
      <%= form.number_field :percentual_desconto %>
    </div>
  
    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
  